@model CoffeeBlend.Web.ViewModels.BlogViewModel.SingleBlogViewModel
@{
    this.ViewData["Title"] = "Details Page";
}

<section class="home-slider owl-carousel">
    <div class="slider-item" style="background-image: url(/images/bg_3.jpg);" data-stellar-background-ratio="0.5">
        <div class="overlay"></div>
        <div class="container">
            <div class="row slider-text justify-content-center align-items-center">
                <div class="col-md-7 col-sm-12 text-center ftco-animate">
                    <h1 class="mb-3 mt-5 bread">Blog Details</h1>
                    <p class="breadcrumbs"><span class="mr-2"><a asp-area="" asp-controller="Home" asp-action="Index">Home</a></span> <span class="mr-2"><a asp-area="" asp-controller="Blog" asp-action="All">Blog</a></span> <span>Blog Single</span></p>
                </div>
            </div>
        </div>
    </div>
</section>

<section class="ftco-section">
    <div class="container">
        <div class="row">
            <div class="col-md-8 ftco-animate">
                <h2 class="mb-3">@Model.Title</h2>
                <img src="@Model.ImageUrl" alt="" class="img-fluid">
                <h3></h3>
                <p>@Model.Content</p>

                <div class="about-author d-flex">
                    <div class="desc align-self-md-center">
                        <h3>Author @Model.AuthorName</h3>
                    </div>
                </div>

                <div class="pt-5 mt-5">
                    <h3 class="mb-5">Comments</h3>
                    <ul class="comment-list">
                        <li class="comment">
                            @foreach (var comment in Model.Comments)
                            {
                                <div class="comment-body">
                                    <h3>@comment.User.UserName</h3>
                                    <div class="meta">@comment.CreatedOn.ToShortDateString()</div>
                                    <p>@comment.Content</p>
                                </div>
                            }
                        </li>
                    </ul>
                    <!-- END comment-list -->

                    <div class="comment-form-wrap pt-5">
                        <h3 class="mb-5">Leave a comment</h3>
                        <form method="post">
                            <div class="form-group">
                                <label for="message">Message</label>
                                <textarea name="" id="message" cols="30" rows="5" class="form-control"></textarea>
                            </div>
                            <div class="form-group">
                                <input type="submit" value="Post Comment" class="btn py-3 px-4 btn-primary">
                            </div>
                        </form>
                    </div>
                </div>
                <partial name="_ReacentBlogsPartial" model="@Model"/>
            </div> <!-- .col-md-8 -->
        </div>
    </div>
</section> <!-- .section -->